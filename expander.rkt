#lang racket

#;(require (prefix-in tr: typed/racket)
         (for-syntax syntax/parse))

#;(provide
 (ename-out [module-begin/apl3d #%module-begin]
            [top-interaction/apl3d #%top-interaction]))

#;(define-syntax (module-begin/apl3d stx)
  #f)

#;(define-syntax (top-interaction/apl3d stx)
  #f)